sudo: required
services:
  - docker

before_install:
  - docker build -t amarchino/docker-react -d Dockerfile.dev .

script:
  - docker run -e CI=true amarchino/docker-react npm run test -- --coverage